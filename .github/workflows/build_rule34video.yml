name: Build Rule34Video

on:
  push:
    branches: [ main, master ]
    paths:
      - 'src/pt/rule34video/**'
      - '.github/workflows/build_rule34video.yml'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'src/pt/rule34video/**'
  workflow_dispatch:

# ⬇️ NECESSÁRIO para o softprops/action-gh-release criar Releases
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache do Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Permissão de execução no gradlew
        run: chmod +x ./gradlew

      - name: Compilar a extensão Rule34Video
        run: ./gradlew :src:pt:rule34video:assembleDebug --no-daemon --stacktrace

      - name: Localizar APK gerado
        id: find_apk
        run: |
          APK_PATH=$(find src/pt/rule34video/build/outputs/apk -name "*.apk" | head -n 1)
          echo "apk_path=$APK_PATH" >> $GITHUB_OUTPUT
          echo "APK encontrado em: $APK_PATH"

      - name: Upload do APK como artefato
        uses: actions/upload-artifact@v4
        with:
          name: rule34video-apk-${{ github.run_number }}
          path: ${{ steps.find_apk.outputs.apk_path }}
          if-no-files-found: error
          retention-days: 30

      - name: Upload em caso de falha (logs)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            **/build/reports/**
            **/build/outputs/logs/**
          if-no-files-found: ignore

      - name: Criar Release (apenas push em main/master)
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: rule34video-v1.0.${{ github.run_number }}
          name: Rule34Video v1.0.${{ github.run_number }}
          files: ${{ steps.find_apk.outputs.apk_path }}
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
